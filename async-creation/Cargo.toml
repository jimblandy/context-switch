[package]
name = "async-creation"
version = "0.1.0"
authors = ["Jim Blandy <jimb@red-bean.com>"]
edition = "2018"

[dependencies]
async-std = "1.6.0-beta.2"
docopt = "1"
serde = { version = "1", features = ["derive"] }
utils = { path = "../utils" }
